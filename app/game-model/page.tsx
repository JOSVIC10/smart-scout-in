export default function GameModelPage() {
  return (
    <main className="min-h-screen p-8" style={{ background: "var(--background)" }}>
      <h1 className="text-4xl font-black" style={{ color: "var(--foreground)" }}>
        🗺️ Modelo de Juego
      </h1>
      <p className="mt-2" style={{ color: "var(--muted-foreground)" }}>
        Editor táctico visual con canvas interactivo — próximamente
      </p>
    </main>
  );
}
